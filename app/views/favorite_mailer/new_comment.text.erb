New comment to "<%= @post.title %>"
 <%= "=" * (@post.title.length + 12) %>

 By: <%= @comment.user.email %>

 "<%= @comment.body.first(30) %> ..."

 Visit online at: <%= place_post_url(@post.place, @post, anchor: "comment-#{@comment.id}") %>
